import { createStore } from 'vuex'
import auth from './modules/auth';
import arquivo from './modules/arquivos';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth,
    arquivo
  }
})
